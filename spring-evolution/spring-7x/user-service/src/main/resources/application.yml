server:
  port: 8081

spring:
  application:
    name: user-service
  threads:
    virtual:
      enabled: true
  mvc:
    apiversion:
      use:
        header: X-API-Version
      supported: ["1", "2"]
      required: true
      detect-supported: true
  datasource:
    url: jdbc:postgresql://postgres:5432/users
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

management:
  endpoints:
    web:
      exposure:
        include: health,info
