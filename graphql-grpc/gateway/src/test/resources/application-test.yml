grpc:
  server:
    enabled: false


spring:
#  autoconfigure:
#    exclude: org.springframework.grpc.server.autoconfigure.GrpcServerAutoConfiguration
  main:
    web-application-type: none
  cloud:
    consul:
      discovery:
        enabled: false
      config:
        enabled: false

dgs:
  graphql:
    schema-locations: classpath:/schema/**/*.graphql*